@extends('layouts.app')

@section('content')
    <div class="container">
        <div class="row">

            <div class="col-md-9">
                <div class="card">
                    <div class="card-header">NewsFeed {{ $newsfeed->id }}</div>
                    <div class="card-body">

                        <a href="{{ url('/news-feed') }}" title="Back"><button class="btn btn-warning btn-sm"><i class="fa fa-arrow-left" aria-hidden="true"></i> Back</button></a>
                        <a href="{{ url('/news-feed/' . $newsfeed->id . '/edit') }}" title="Edit NewsFeed"><button class="btn btn-primary btn-sm"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</button></a>

                        <form method="POST" action="{{ url('newsfeed' . '/' . $newsfeed->id) }}" accept-charset="UTF-8" style="display:inline">
                            {{ method_field('DELETE') }}
                            {{ csrf_field() }}
                            <button type="submit" class="btn btn-danger btn-sm" title="Delete NewsFeed" onclick="return confirm(&quot;Confirm delete?&quot;)"><i class="fa fa-trash-o" aria-hidden="true"></i> Delete</button>
                        </form>
                        <br/>
                        <br/>

                        <div class="table-responsive">
                            <table class="table">
                                <tbody>
                                    <tr>
                                        <th>ID</th><td>{{ $newsfeed->id }}</td>
                                    </tr>
                                    <tr><th> Content </th><td> {{ $newsfeed->content }} </td></tr><tr><th> Image </th><td> {{ $newsfeed->image }} </td></tr><tr><th> Video </th><td> {{ $newsfeed->video }} </td></tr>
                                </tbody>
                            </table>
                        </div>

                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
